package com.example.downloadmanager.Model;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;

import com.example.downloadmanager.R;

public class SelectLib extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_select_lib);

/*        public void btnImagemLib(View view) {
            Intent image = new Intent(getApplicationContext(),InsertURLimag.class);
            startActivity(image);
        }*/

        /*public void btnMusicLib (View view){
            Intent music = new Intent(getApplicationContext(), InsertURLmusic.class);
            startActivity(music);
        }*/
    }
}